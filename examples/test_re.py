import re

def test_re():
    """Test re module flags."""
    i_flag = re.IGNORECASE
    m_flag = re.MULTILINE
    s_flag = re.DOTALL
    x_flag = re.VERBOSE
    a_flag = re.ASCII
    i_short = re.I
    m_short = re.M
    return i_flag
